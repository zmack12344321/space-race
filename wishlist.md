# Wishlist

## Lobby name → floating block letters
Implement a feature in the lobby where, when a player types their name, it becomes a
real object in the lobby scene — floating block letters (a 3D text mesh near/above the
player) that displays the entered name. Should update live as the name changes and be
styled to fit the lunar-night vibe (e.g., emissive, gently bobbing).
